<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectfuelfree">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".top_up_info_activity.TopUpInfoActivity"></activity>
        <activity android:name=".check_history_activity.CheckHistoryActivity" />
        <activity android:name=".purchase_activity.AfterPurchaseActivity" />
        <activity android:name=".purchase_activity.PurchaseProductActivity" />
        <activity android:name=".purchase_activity.DisplayProductsActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".user_register_login.RegisterActivity" />
        <activity android:name=".user_register_login.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>